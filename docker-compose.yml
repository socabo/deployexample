version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - ENVIRONMENT=development
    volumes:
      - .:/app
    command: python3 -m flask run --host=0.0.0.0 --reload